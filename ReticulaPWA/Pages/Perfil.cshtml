@page "/perfil"
@model ReticulaPWA.Pages.PerfilModel
<div class="perfil__container">
    <section class="perfil">
        <section class="perfil__fondo">
            <div class="perfil__fondo__color"></div>
            <figure class="perfil__foto">
                <img id="perfil__fotografia"
                     alt="Foto de perfil"
                     class="perfil__img" />
            </figure>
        </section>

        <section class="perfil__info">
            <label class="perfil__nombre" id="perfil__nombre"></label>
            <label class="perfil__numcontrol" id="perfil__numControl"></label>
        </section>

        <section class="perfil__info-general">
            <h4 class="informacion__titulo">Información general</h4>

            <div class="perfil__contenido__informacion" id="contenedor__informacion">
                <template id="plantilla__informacion">
                    <div class="informacion__div">
                        <label class="informacion__label">Carrera</label>
                        <label class="informacion__dato">Ingeniería en Sistemas Computacionales</label>
                    </div>
                </template>

            </div>
        </section>
    </section>
</div>

<script>

    const plantilla = document.getElementById('plantilla__informacion');
    const nombreLbl = document.getElementById('perfil__nombre');
    const numControlLbl = document.getElementById('perfil__numControl');
    const fotoPerfil = document.getElementById('perfil__fotografia');
    const contenedorInformacion = document.getElementById("contenedor__informacion")
    const listaInformacion = document.createDocumentFragment();

    const informacionChannel = new BroadcastChannel('informacion_recibida');


    informacionChannel.onmessage = (event =>{

        const perfil = event.data;





    }





</script>
